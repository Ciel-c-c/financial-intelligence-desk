import type{LiveNewsItem}from'../data/newsFeedTypes';export function NewsSourcePanel({item}:{item:LiveNewsItem}){return <section className="reading-card source-panel"><h2>来源与验证</h2><p><strong>{item.verificationStatus==='official'?'官方原始发布':item.verificationStatus==='cross-checked'?'多来源交叉验证':'可靠机构来源'}</strong></p><a href={item.canonicalUrl} target="_blank" rel="noreferrer">查看原始来源</a>{item.relatedSources.map(source=><a key={source.url} href={source.url} target="_blank" rel="noreferrer">{source.name}</a>)}</section>}
