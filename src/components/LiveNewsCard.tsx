import {Link} from 'react-router-dom';import type{LiveNewsItem}from'../data/newsFeedTypes';
export function LiveNewsCard({item}:{item:LiveNewsItem}){const tags=[...item.analysisLevels,...item.eventTypes,...item.impactChannels,...item.regions].slice(0,5);return <article className="news-card live-news-card"><div className="news-tags">{tags.map(tag=><span className="topic" key={tag}>{tag}</span>)}</div><Link to={`/news/${item.id}`}><h3>{item.titleZh??item.originalTitle}</h3></Link><p>{item.summaryZh??item.originalSummary??'查看原始来源了解详情。'}</p><footer><a href={item.canonicalUrl} target="_blank" rel="noreferrer">{item.sourceName}{item.translationStatus==='generated'?' · 非官方中文整理':''} ↗</a><time dateTime={item.publishedAt}>新闻发布：{new Date(item.publishedAt).toLocaleString('zh-CN',{timeZone:'Asia/Shanghai'})}</time></footer></article>}
