import{Link}from'react-router-dom';import type{LiveNewsItem}from'../data/newsFeedTypes';import{AnalysisBlock}from'./AnalysisBlock';import{NewsSourcePanel}from'./NewsSourcePanel';
export function LiveNewsDetail({item}:{item:LiveNewsItem}){const tags=[...item.analysisLevels,...item.eventTypes,...item.impactChannels,...item.regions];return <main className="page detail-page"><Link className="back-link" to="/">← 返回今日</Link><article className="article-head"><div className="news-tags">{tags.map(tag=><span className="topic" key={tag}>{tag}</span>)}</div><h1>{item.titleZh??item.originalTitle}</h1><p className="lead">{item.summaryZh??item.originalSummary}</p>{item.translationStatus==='generated'&&<p className="eyebrow">中文整理</p>}{item.titleZh&&item.titleZh!==item.originalTitle&&<p>原标题：{item.originalTitle}</p>}<div className="source-row"><span>{item.sourceName} · {new Date(item.publishedAt).toLocaleString('zh-CN',{timeZone:'Asia/Shanghai'})}</span></div></article><NewsSourcePanel item={item}/><div className="analysis-grid"><AnalysisBlock title="事实" tone="fact" items={item.facts}/><AnalysisBlock title="市场预期" tone="consensus" items={item.expectations}/>{item.inferences.length>0&&<AnalysisBlock title="推断" tone="inference" items={item.inferences}/>}</div></main>}
