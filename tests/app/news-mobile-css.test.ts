import{readFileSync}from'node:fs';import{describe,expect,it}from'vitest';describe('live news responsive CSS',()=>{it('keeps filters touch friendly and tags scrollable',()=>{const css=readFileSync('src/styles/global.css','utf8');expect(css).toContain('.news-tags{display:flex');expect(css).toContain('overflow-x:auto');expect(css).toContain('.news-filters label,.news-filters select,.news-filters button{width:100%}');});});
